# Local development override.
# This file is NOT committed to git (it's in .gitignore by default).
# Remove or comment out this line to use the deployed Render backend instead.
VITE_API_URL=http://localhost:5000
